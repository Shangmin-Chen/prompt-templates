# refresh — legacy codebase audit roadmap

Agent prompt for auditing a legacy codebase and producing a
prioritized refactoring roadmap inside `<report>` tags, with a strict
tool-use contract and real citations.

The full prompt is in [`refresh.md`](./refresh.md).
